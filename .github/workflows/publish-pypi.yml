name: Publish Python 🐍 distribution to PyPI

on:
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      publish_to:
        description: 'Publish to PyPI, TestPyPI, or both'
        required: true
        type: choice
        options:
          - 'pypi'
          - 'testpypi'
          - 'both'
        default: 'pypi'

jobs:
  build:
    name: Build Python distribution 📦
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v6
        with:
          # Fetch all history for setuptools_scm to determine version from git tags
          fetch-depth: 0

      - name: Set up Python 🐍
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install build dependencies
        run: |
          python -m pip install --upgrade pip
          pip install build setuptools_scm

      - name: Verify version extraction
        run: |
          python -m setuptools_scm
          echo "Package version will be: $(python -m setuptools_scm)"

      - name: Build distribution packages
        run: python -m build

      - name: Store distribution packages
        uses: actions/upload-artifact@v7
        with:
          name: python-package-distributions
          path: dist/
          if-no-files-found: error

  publish-to-testpypi:
    name: Publish to TestPyPI 🧪
    # Only run on manual dispatch with testpypi/both
    if: |
      github.event_name == 'workflow_dispatch' &&
      (github.event.inputs.publish_to == 'testpypi' || github.event.inputs.publish_to == 'both')
    needs:
      - build
    runs-on: ubuntu-latest
    environment:
      name: testpypi
      url: https://test.pypi.org/p/reverse-engineering-assistant
    permissions:
      id-token: write  # IMPORTANT: mandatory for trusted publishing
    steps:
      - name: Download distributions
        uses: actions/download-artifact@v8
        with:
          name: python-package-distributions
          path: dist/

      - name: Publish to TestPyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          repository-url: https://test.pypi.org/legacy/
          skip-existing: true
          verbose: true

  publish-to-pypi:
    name: Publish to PyPI 🚀
    # Only run on release publish or manual dispatch with pypi/both
    if: |
      github.event_name == 'release' ||
      (github.event_name == 'workflow_dispatch' &&
       (github.event.inputs.publish_to == 'pypi' || github.event.inputs.publish_to == 'both'))
    needs:
      - build
    runs-on: ubuntu-latest
    environment:
      name: pypi
      url: https://pypi.org/p/reverse-engineering-assistant
    permissions:
      contents: write  # IMPORTANT: mandatory for GitHub Releases
      id-token: write  # IMPORTANT: mandatory for trusted publishing and sigstore
    steps:
      - name: Download distributions
        uses: actions/download-artifact@v8
        with:
          name: python-package-distributions
          path: dist/

      - name: Sign distributions with Sigstore
        uses: sigstore/gh-action-sigstore-python@v3.2.0
        with:
          inputs: >-
            ./dist/*.tar.gz
            ./dist/*.whl
          upload-signing-artifacts: true

      - name: Upload signatures to GitHub Release
        if: github.event_name == 'release'
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: |
          # Upload Python package signatures to existing release
          gh release upload '${{ github.event.release.tag_name }}' ./dist/*.sigstore* --clobber \
            --repo '${{ github.repository }}'

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          verbose: true
